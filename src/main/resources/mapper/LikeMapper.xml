<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rebound.mapper.LikeMapper">
    <insert id="insertLike">
        insert into tbl_like(member_id, comment_id)
        values (#{memberId}, #{commentId})
    </insert>

    <delete id="deleteLike">
        delete from tbl_like
        where member_id = #{memberId} and comment_id = #{commentId}
    </delete>

    <select id="countByCommentId" resultType="int">
        select count(*)
        from tbl_like
        where comment_id = #{commentId}
    </select>

    <select id="checkLiked">
        select id, member_id, comment_id, created_date, updated_date
        from tbl_like
        where member_id = #{memberId} and comment_id = #{commentId}
        limit 1
    </select>
</mapper>
