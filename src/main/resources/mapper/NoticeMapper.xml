<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rebound.mapper.NoticeMapper">
    <select id="selectNoticeAll">
        select * from tbl_notice
        <if test="keyword != null">
            where notice_title like concat('%', #{keyword}, '%') or
            notice_content like concat('%', #{keyword}, '%')
        </if>
        order by id desc
        limit #{memberCriteria.rowCount} offset #{memberCriteria.offset}

    </select>
    <select id="selectNoticeAllCount">
        select count(*) from tbl_notice
        <if test="keyword != null">
            where notice_title like concat('%', #{keyword}, '%') or
            notice_content like concat('%', #{keyword}, '%')
        </if>
    </select>
    <select id="selectNoticeById">
        select * from tbl_notice
        where id = #{id}
    </select>
</mapper>