<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rebound.mapper.CommentMapper">
    <insert id="insertComment">
        insert into tbl_comment(comment_content, member_id, post_id)
        values (#{commentContent}, #{memberId}, #{postId})
    </insert>

    <select id="selectCommentsByPostId">
        select member_id, member_name, id, comment_content, post_id, created_date, updated_date
        from view_comment_member
        where post_id = #{postId} and comment_status = 'active'
        order by created_date desc
    </select>
    
    <select id="selectCommentsCountByPostId">
        select count(*)
        from tbl_comment
        where post_id = #{postId} and comment_status = 'active'
    </select>
    <select id="selectAllByKeyword">
        select p.post_title, c.comment_content, c.created_date
        from tbl_member m join tbl_comment c
                               on m.id = c.member_id
        join tbl_post p on m.id = p.member_id
        where p.post_status = 'active'
          and (m.member_email like concat('%', #{keyword}, '%') or m.member_Name like concat('%', #{keyword}, '%'))
        order by p.created_date desc
    </select>

    <select id="selectCountAllByKeyword">
        select count(*)
        from tbl_member m join tbl_comment c
                               on m.id = c.member_id
                          join tbl_post p on m.id = p.member_id
        where p.post_status = 'active'
          and (m.member_email like concat('%', #{keyword}, '%') or m.member_Name like concat('%', #{keyword}, '%'))

    </select>

    <update id="updateComment">
        update tbl_comment
        set comment_content = #{commentContent}, updated_date = current_timestamp
        where id = #{id}
    </update>

    <update id="delete">
        update tbl_comment
        set comment_status = 'inactive'
        where id = #{id}
    </update>
</mapper>