<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rebound.mapper.MainPageMapper">
    <select id="postPreview">
        select id, post_content, post_title, post_views, created_date, updated_date, member_id
        from tbl_post
        order by id desc
        limit 6 offset 0
    </select>

    <select id="todayMessagePreview">
        select id, today_message_content, counselor_id
        from tbl_today_message
        order by id desc
        limit 12 offset 0
    </select>

    <select id="noticePreview">
        select id, notice_title, notice_content
        from tbl_notice
        order by id desc
        limit 7 offset 0
    </select>

    <select id="selectCountAllSearchMainPage">
        select count(*) from tbl_post
        where post_status = 'active'
        <if test="keyword != null">
            and (
            post_title like concat('%', #{keyword}, '%') or
            post_content like concat('%', #{keyword}, '%')
            )
        </if>
    </select>

    <select id="selectPostFromMainPage">
        select id, post_content, post_title, post_views, created_date, updated_date, member_id, post_status, category_id
        from tbl_post
        where post_status = 'active'
        <if test="keyword != null">
            and (
            post_title like concat('%', #{keyword}, '%') or
            post_content like concat('%', #{keyword}, '%')
            )
        </if>
        order by id desc
    </select>
</mapper>